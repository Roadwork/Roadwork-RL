/**
 * Convert degrees to radians
 * @param {number} degrees 
 */
exports.radians = (degrees) => degrees * Math.PI / 180;

/**
 * Convert radians to degrees
 * @param {number} radians 
 */
exports.degrees = (radians) => radians * 180 / Math.PI;